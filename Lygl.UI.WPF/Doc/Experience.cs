using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Lygl.UI.Doc
{
    class Experience
    {
        #region ICommand
        //命令源、命令语义、命令逻辑、命令目标
        //    命令订阅CanExecuteChanged事件，通过CanExecute方法判断是否可执行命令
        //命令逻辑是与目标统一的，不同的目标，结果不一样
        //许多控件内置对预定义命令的支持，如TextBox支持copy paste、Menu作为命令源的支持等

//        WPF 中的路由命令模型可以分为四个主要概念：命令、命令源、命令目标以及命令绑定：

//命令是要执行的操作。

        //命令源是调用命令的对象。                      WPF 中的命令源通常实现 ICommandSource 接口。 

//命令目标是在其上执行命令的对象。

//命令绑定是将命令逻辑映射到命令的对象。



        #endregion


    }
}
